# AI 编辑高级功能增量验收（2026-08-01）

| 功能 | 状态 | 实现与限制 |
|---|---|---|
| Token 预算 | 通过 | 支持 4K–64K 自定义预算，显示当前活动文档估算值，并约束请求输出上限。 |
| 长上下文压缩 | 部分通过 | 文件按活动文件、已打开文件、候选文件分级；超预算时省略低优先级文件，单个超大活动文件会截断并标记。尚未实现模型驱动的历史对话摘要。 |
| 待审候选重启恢复 | 通过 | 多文件候选和指令按工作区持久化；重新打开工作区自动恢复，接受或拒绝完毕后自动清理草稿。 |
| 符号/引用/import 搜索 | 部分通过 | 新增跨工作区语义搜索入口，结果区分 definition、reference、import，并显示 import 来源。当前采用多语言结构化词法索引；尚未接入 TypeScript/Python Language Server，因此重载、别名和动态引用可能不完整。 |

自动化结果：TypeScript 通过；18 个测试文件、125 项测试全部通过。
