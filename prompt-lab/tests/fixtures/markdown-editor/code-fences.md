# 代码块

普通围栏：

```js
const greet = (name) => `Hello, ${name}!`;
console.log(greet('World'));
```

带语言：

```python
def fib(n):
    if n < 2:
        return n
    return fib(n - 1) + fib(n - 2)

print([fib(i) for i in range(10)])
```

行内代码：`const x = 1;` 与 `let y = 2` 混用。
