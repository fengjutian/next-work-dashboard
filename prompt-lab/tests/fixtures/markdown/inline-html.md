# Inline HTML

This paragraph has <kbd>Ctrl</kbd>+<kbd>S</kbd> for save.

Use <span class="highlight">inline spans</span> for emphasis.

A <a href="https://example.com" title="example">link with HTML</a> embedded.

Sub <sub>2</sub> and sup <sup>n</sup> notation.

That's all for inline HTML.
