# ContractGuard

Local-first contract review plugin for next-work-dashboard. It parses PDF, DOCX and text contracts, extracts clauses, applies deterministic risk rules, links each finding to evidence, and exports Markdown/JSON reports.

The package is UI-host agnostic and does not upload contract content. OCR and model-assisted review are extension points for later versions. Rule output is decision support, not legal advice.
