/// Centralized route names so widgets never have to hard-code strings.
class Routes {
  static const pairing = '/';
  static const casting = '/casting';
  static const transfer = '/transfer';
  static const settings = '/settings';
}
