name: Quality

on:
  push:
  pull_request:

jobs:
  check:
    runs-on: windows-latest
    defaults:
      run:
        working-directory: prompt-lab
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: prompt-lab/package-lock.json
      - run: npm ci
      - run: npm run check
